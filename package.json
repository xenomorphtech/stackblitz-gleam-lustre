{
  "dependencies": {
    "concurrently": "^7.5.0",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "vite": ">=2.9.13"
  },
  "scripts": {
    "build": "node pkg/build.mjs"
  },
  "stackblitz": {
    "startCommand": "npm install && npm build && npm exec vite ",
    "compileTrigger": "save"
  }
}

